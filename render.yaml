services:
  - type: web
    name: ring
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: POLL_INTERVAL_MINUTES
        value: 15
      - key: ALERT_MAX_PER_DAY
        value: 5
